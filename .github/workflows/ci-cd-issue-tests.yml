# name: CI/CD Issue Tests

# on:
#   push:
#     branches: [ main, develop ]
#   pull_request:
#     branches: [ main, develop ]
#   workflow_dispatch:

# jobs:
#   ci-cd-issue-tests:
#     name: Run CI/CD Issue Tests
#     runs-on: ubuntu-latest
#     timeout-minutes: 30

#     services:
#       postgres:
#         image: postgres:15
#         env:
#           POSTGRES_USER: postgres
#           POSTGRES_PASSWORD: postgres
#           POSTGRES_DB: windx_test
#         ports:
#           - 5432:5432
#         options: >-
#           --health-cmd pg_isready
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'

#       - name: Install uv
#         uses: astral-sh/setup-uv@v5
#         with:
#           enable-cache: true
#           cache-dependency-glob: "uv.lock"

#       - name: Install dependencies
#         run: |
#           echo "📦 Installing dependencies with uv..."
#           uv sync --all-extras --dev
#           echo "✅ Dependencies installed"

#       - name: Verify PostgreSQL connection
#         run: |
#           echo "🔍 Verifying PostgreSQL connection..."
#           sudo apt-get install -y postgresql-client
#           PGPASSWORD=postgres psql -h localhost -U postgres -d windx_test -c "SELECT version();"
#           echo "✅ PostgreSQL is ready"

#       - name: Create LTREE extension
#         run: |
#           echo "🌳 Creating LTREE extension..."
#           PGPASSWORD=postgres psql -h localhost -U postgres -d windx_test -c "CREATE EXTENSION IF NOT EXISTS ltree;"
#           echo "✅ LTREE extension created"

#       - name: Create .env.test for CI
#         run: |
#           cat > .env.test << EOF
#           # Application
#           APP_NAME="Backend API Test"
#           APP_VERSION="1.0.0"
#           DEBUG=True
#           API_V1_PREFIX="/api/v1"

#           # Database Configuration - Local PostgreSQL for CI
#           DATABASE_PROVIDER=postgresql
#           DATABASE_CONNECTION_MODE=direct
#           DATABASE_HOST=localhost
#           DATABASE_PORT=5432
#           DATABASE_USER=postgres
#           DATABASE_PASSWORD=postgres
#           DATABASE_NAME=windx_test
#           DATABASE_POOL_SIZE=5
#           DATABASE_MAX_OVERFLOW=5
#           DATABASE_POOL_PRE_PING=True
#           DATABASE_ECHO=False
#           DATABASE_DISABLE_POOLER_WARNING=True

#           # Security - Test keys
#           SECRET_KEY="test-secret-key-for-testing-only-min-32-characters-long"
#           ALGORITHM="HS256"
#           ACCESS_TOKEN_EXPIRE_MINUTES=30

#           # CORS - Allow all for testing
#           BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8000","http://127.0.0.1:3000","http://127.0.0.1:8000"]

#           # Cache (Redis) - Disabled for tests
#           CACHE_ENABLED=False
#           CACHE_REDIS_HOST=localhost
#           CACHE_REDIS_PORT=6379
#           CACHE_REDIS_PASSWORD=
#           CACHE_REDIS_DB=2
#           CACHE_PREFIX=testapi:cache
#           CACHE_DEFAULT_TTL=300

#           # Rate Limiter (Redis) - Disabled for tests
#           LIMITER_ENABLED=False
#           LIMITER_REDIS_HOST=localhost
#           LIMITER_REDIS_PORT=6379
#           LIMITER_REDIS_PASSWORD=
#           LIMITER_REDIS_DB=3
#           LIMITER_PREFIX=testapi:limiter
#           LIMITER_DEFAULT_TIMES=1000
#           LIMITER_DEFAULT_SECONDS=60

#           # Test User Credentials
#           TEST_ADMIN_USERNAME=test_admin
#           TEST_ADMIN_EMAIL=test_admin@example.com
#           TEST_ADMIN_PASSWORD=AdminPassword123!
#           TEST_USER_USERNAME=test_user
#           TEST_USER_EMAIL=test_user@example.com
#           TEST_USER_PASSWORD=UserPassword123!
#           EOF

#           echo "✅ .env.test created successfully for CI"

#       - name: Run database migrations
#         run: uv run alembic upgrade head
#         env:
#           ENV: test
#           DEBUG: "True"
#           DATABASE_HOST: localhost
#           DATABASE_PORT: 5432
#           DATABASE_USER: postgres
#           DATABASE_PASSWORD: postgres
#           DATABASE_NAME: windx_test
#           DATABASE_PROVIDER: postgresql
#           DATABASE_CONNECTION_MODE: direct
#           SECRET_KEY: "test-secret-key-for-testing-only-min-32-characters-long"

#       - name: Run CI/CD Issue Tests
#         run: |
#           echo "🔍 Running tests marked with ci_cd_issue..."
#           echo "These tests pass locally but have known issues in CI/CD environment"
#           echo ""
#           uv run pytest tests/ \
#             --tb=long \
#             --maxfail=1 \
#             --instafail \
#             --junit-xml=ci-cd-issue-results.xml \
#             -m ci_cd_issue \
#             -vv \
#             --disable-warnings
#         continue-on-error: true
#         env:
#           ENV: test
#           DEBUG: "True"
#           DATABASE_HOST: localhost
#           DATABASE_PORT: 5432
#           DATABASE_USER: postgres
#           DATABASE_PASSWORD: postgres
#           DATABASE_NAME: windx_test
#           DATABASE_PROVIDER: postgresql
#           DATABASE_CONNECTION_MODE: direct
#           SECRET_KEY: "test-secret-key-for-testing-only-min-32-characters-long"

#       - name: Upload test results
#         uses: actions/upload-artifact@v4
#         if: always()
#         with:
#           name: ci-cd-issue-test-results
#           path: ci-cd-issue-results.xml
#           retention-days: 30

#       - name: Generate test summary
#         if: always()
#         run: |
#           echo "## 🔍 CI/CD Issue Tests Summary" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "These tests are marked with \`ci_cd_issue\` because they pass locally but fail in CI/CD." >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "**Purpose:** Isolate and debug environment-specific test failures" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
          
#           if [ -f ci-cd-issue-results.xml ]; then
#             echo "### Test Results" >> $GITHUB_STEP_SUMMARY
#             echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
#             grep -o 'tests="[0-9]*"' ci-cd-issue-results.xml | head -1 >> $GITHUB_STEP_SUMMARY || echo "Tests: Unknown" >> $GITHUB_STEP_SUMMARY
#             grep -o 'failures="[0-9]*"' ci-cd-issue-results.xml | head -1 >> $GITHUB_STEP_SUMMARY || echo "Failures: Unknown" >> $GITHUB_STEP_SUMMARY
#             grep -o 'errors="[0-9]*"' ci-cd-issue-results.xml | head -1 >> $GITHUB_STEP_SUMMARY || echo "Errors: Unknown" >> $GITHUB_STEP_SUMMARY
#             echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
#           fi
          
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "### Known Issues" >> $GITHUB_STEP_SUMMARY
#           echo "1. **Form Validation Test**: Expects HTML but gets JSON in CI" >> $GITHUB_STEP_SUMMARY
#           echo "2. **Bulk Operations Tests**: Database isolation/timing issues" >> $GITHUB_STEP_SUMMARY
#           echo "3. **RBAC Workflow Test**: Authentication timing issues" >> $GITHUB_STEP_SUMMARY
#           echo "4. **Entry API Test**: Response format differences" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "### Next Steps" >> $GITHUB_STEP_SUMMARY
#           echo "- Review test output for environment-specific differences" >> $GITHUB_STEP_SUMMARY
#           echo "- Check database transaction timing" >> $GITHUB_STEP_SUMMARY
#           echo "- Verify response format handling" >> $GITHUB_STEP_SUMMARY
#           echo "- Consider adding retry logic or delays where appropriate" >> $GITHUB_STEP_SUMMARY
