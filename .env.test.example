# Application
APP_NAME="Backend API Test"
APP_VERSION="1.0.0"
DEBUG=True
API_V1_PREFIX="/api/v1"

# Database Configuration - Test Database (Supabase PostgreSQL)
# Using Supabase Connection Pooler (IPv4 compatible)
# Pooler endpoint supports IPv4, unlike direct connection (IPv6 only)
DATABASE_PROVIDER=supabase
DATABASE_CONNECTION_MODE=session_pooler
DATABASE_HOST=your-database-host
DATABASE_PORT=5432
DATABASE_USER=postgres.vglmnngcvcrdzvnaopde
DATABASE_PASSWORD=your-database-password
DATABASE_NAME=postgres
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=5
DATABASE_POOL_PRE_PING=True
DATABASE_ECHO=False
DATABASE_DISABLE_POOLER_WARNING=True

# Supabase Configuration (for reference)
SUPABASE_URL=your-supabase-url
SUPABASE_ANON_KEY=your-subapase-anon-key

# Security - Test keys
SECRET_KEY="test-secret-key-for-testing-only-min-32-characters-long"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS - Allow all for testing
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8000","http://127.0.0.1:3000","http://127.0.0.1:8000"]

# Cache (Redis) - Disabled for tests
CACHE_ENABLED=False
CACHE_REDIS_HOST=localhost
CACHE_REDIS_PORT=6379
CACHE_REDIS_PASSWORD=
CACHE_REDIS_DB=2
CACHE_PREFIX=testapi:cache
CACHE_DEFAULT_TTL=300

# Rate Limiter (Redis) - Disabled for tests
LIMITER_ENABLED=False
LIMITER_REDIS_HOST=localhost
LIMITER_REDIS_PORT=6379
LIMITER_REDIS_PASSWORD=
LIMITER_REDIS_DB=3
LIMITER_PREFIX=testapi:limiter
LIMITER_DEFAULT_TIMES=1000
LIMITER_DEFAULT_SECONDS=60
